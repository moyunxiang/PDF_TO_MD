# Core: PDF → Markdown conversion (pulls torch, transformers, surya-ocr, etc.)
marker-pdf>=1.6

# API calls (OpenRouter via OpenAI-compatible SDK)
openai>=1.0

# Terminal UI
rich>=13.0
simple-term-menu>=1.0

# PDF manipulation (split, page count, render)
pypdfium2>=4.0
